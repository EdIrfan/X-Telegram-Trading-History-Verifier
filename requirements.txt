# Scraper runtime deps (the AI may add more into a venv per analysis as needed).
playwright>=1.45        # X/Twitter browser automation (Chromium)
requests>=2.31          # Binance klines price oracle (scripts/prices.py)
Telethon>=1.36          # Telegram channel scraping (MTProto)

# Reading the AI's reports without VS Code:
markdown>=3.5           # scripts/md2html.py -> standalone .html for any browser
rich>=13.0              # `python -m rich.markdown REPORT.md` -> rendered in the terminal
